# Gemini
GEMINI_API_KEY=

# Mongo (use full read-write creds — we enforce read-only at the tool layer)
MONGO_URI=

# Single shared admin password — change after first deploy
OPS_BOT_PASSWORD=changeme

# Local clone roots for read_file / bash tools (refreshed by scripts/sync-repos.js)
REPO_ROOT=/Users/abhinayreddy/Desktop/sagenex-all/sagenex-ops-bot/repos
